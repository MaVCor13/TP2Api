import React from 'react';

// Componente para mostrar el clima actual
const CurrentWeather = ({ currentWeather }) => {
  return (
    <div>
      {/* Muestra los datos del clima actual aquí */}
    </div>
  );
};
